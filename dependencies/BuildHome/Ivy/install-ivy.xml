<project xmlns:ivy="antlib:org.apache.ivy.ant" name="DownloadAndInstallIvy" xmlns:m="http://ant.apache.org/ivy/maven" default="init-ivy">

  <property name="ivy.install.version" value="2.1.0-rc2" />
  <condition property="ivy.home" value="${env.IVY_HOME}">
    <isset property="env.IVY_HOME" />
  </condition>
  <property name="ivy.home" value="${user.home}/.ant" />
  <property name="ivy.jar.dir" value="${ivy.home}/lib" />
  <property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />

  <property name="download-url" value="http://repo2.maven.org/maven2/org/apache/ivy/ivy" />

  <target name="check-should-download-ivy">
    <condition property="download.ivy" else="false">
      <not>
        <available file="${ivy.jar.file}" />
      </not>
    </condition>
    <echo>${download.ivy}</echo>
  </target>

  <target name="download-ivy" depends="check-should-download-ivy" if="${download.ivy}" >
    <mkdir dir="${ivy.jar.dir}"/>
    
    <!-- download Ivy from web site so that it can be used even without any special installation -->
    <get src="${download-url}/${ivy.install.version}/ivy-${ivy.install.version}.jar" 
         dest="${ivy.jar.file}" usetimestamp="true"/>
  </target>

  <target name="init-ivy" depends="download-ivy">
  <!-- try to load ivy here from ivy home, in case the user has not already dropped
  it into ant's lib dir (note that the latter copy will always take precedence).
  We will not fail as long as local lib dir exists (it may be empty) and
  ivy is in at least one of ant's lib dir or the local lib dir. -->
    <path id="ivy.lib.path">
      <fileset dir="${ivy.jar.dir}" includes="*.jar"/>
    </path>
    <taskdef resource="org/apache/ivy/ant/antlib.xml"
             uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path"/>
  </target> 
</project>